{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fablab-imperia.github.io/meshbee/contract/mqtt-payload.schema.json",
  "title": "MeshBee MQTT payload (placeholder)",
  "description": "Placeholder fallback. The authoritative schema is published by meshbee-server and fetched from its latest release at docs build time.",
  "type": "object",
  "required": ["v", "id"],
  "properties": {
    "v": {
      "type": "integer",
      "minimum": 1,
      "description": "Payload major version. The server decides compatibility per message from this integer."
    },
    "id": {
      "type": "string",
      "description": "Node identifier, e.g. \"hive-03\"."
    },
    "t": {
      "type": "number",
      "description": "Temperature in °C."
    },
    "rh": {
      "type": "number",
      "description": "Relative humidity in %."
    },
    "w": {
      "type": "number",
      "description": "Hive weight in grams."
    },
    "vbat": {
      "type": "number",
      "description": "Battery voltage in volts."
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "Reading timestamp (RFC 3339). Absent when the node has no clock sync; the server stamps arrival time instead."
    }
  },
  "additionalProperties": true
}
