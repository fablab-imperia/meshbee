{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fablab-imperia.github.io/meshbee/contract/mqtt-payload.schema.json",
  "title": "MeshBee MQTT payload",
  "description": "One reading published by a node to beehive/<id_nodo>/data. Maintained by hand in the meshbee umbrella repository until meshbee-server generates it, mirroring what mqtt_handler/payload.py accepts. No field carries a payload version yet; see the contract docs for the planned versioning scheme.",
  "type": "object",
  "properties": {
    "id_nodo": {
      "type": "string",
      "maxLength": 50,
      "description": "Node identifier. Optional in the body: it falls back to the <id_nodo> segment of the topic, and the body wins when both are present. A message is rejected only when neither supplies one."
    },
    "id_sensore": {
      "type": "string",
      "maxLength": 50,
      "description": "Identifies the hive on that node. An unknown value provisions a new hive; when absent, the node's first hive is used."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Reading time, ISO 8601. A trailing Z is accepted. Absent or unparseable values fall back to server time."
    },
    "temperatura": {
      "type": "number",
      "minimum": -50,
      "maximum": 100,
      "description": "Temperature in °C. Out of range drops the whole message."
    },
    "umidita": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "Relative humidity in %. Out of range drops the whole message."
    },
    "peso": {
      "type": "number",
      "minimum": 0,
      "description": "Hive weight in kg. Out of range drops the whole message."
    },
    "dati_raw": {
      "type": "object",
      "description": "Anything else the firmware wants to keep — battery voltage, RSSI, and so on. Stored verbatim as JSONB.",
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
