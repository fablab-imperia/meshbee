{
  "openapi": "3.1.0",
  "info": {
    "title": "MeshBee Server API (placeholder)",
    "version": "0.0.0",
    "description": "Placeholder fallback. The real spec is generated by meshbee-server and fetched from its latest release at docs build time. Until that repo cuts a release, this file is what gets published."
  },
  "servers": [
    {
      "url": "https://api.example.org/v1",
      "description": "Placeholder server"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Health check",
        "operationId": "getHealth",
        "responses": {
          "200": {
            "description": "Service is healthy",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "examples": ["ok"]
                    }
                  },
                  "required": ["status"]
                }
              }
            }
          }
        }
      }
    }
  }
}
