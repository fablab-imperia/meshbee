---
title: API reference
---
<script>document.body.dataset.mdPage = "contract-api";</script>

# API reference

Rendered from the OpenAPI spec generated by `meshbee-server`. The spec file
(`openapi.json`) is fetched from the latest server release at build time.

<div id="app"></div>

<script src="https://cdn.jsdelivr.net/npm/@scalar/api-reference"></script>
<script>
  Scalar.createApiReference('#app', {
    // Canonical absolute URL so the English page and the Italian-chrome mirror
    // both load the single real artifact. English-only by design. On local
    // `mkdocs serve` this fetches the deployed spec (renders once the site is live).
    url: 'https://fablab-imperia.github.io/meshbee/contract/openapi.json',
    theme: 'default',
    hideModels: false,
  })
</script>
