---
title: Overview
---

# The contract

The four MeshBee components ship independently. What keeps them interoperable is this contract:
the MQTT payload format and the HTTP API. It is deliberately **small, explicit, and versioned**.

This site is its **single home**. The artifacts below live at stable URLs that other repos can
link to. Release notes should point here rather than paste a copy — a duplicated spec is a spec
that goes stale without anyone noticing.

!!! note "English only, by design"

    Contract pages are not translated. They describe machine-facing specs and schemas, where a
    second wording is a second source of truth waiting to disagree with the first.

## Artifacts

| Artifact | Where it comes from |
| --- | --- |
| [API reference](api.md) | Generated by `meshbee-server`, rendered with [Scalar](https://scalar.com), fetched from the latest server release at build time |
| [MQTT payload](mqtt-payload.md) | JSON Schema, fetched from the latest server release at build time |
| [Compatibility matrix](compatibility.md) | Maintained here by hand — it's a record of what was field-tested together |

Direct links to the raw files:

- <https://fablab-imperia.github.io/meshbee/contract/openapi.json>
- <https://fablab-imperia.github.io/meshbee/contract/mqtt-payload.schema.json>

## About the placeholders

The generated artifacts are never hand-copied into this repo. The docs build fetches them from
the latest [`meshbee-server`](https://github.com/fablab-imperia/meshbee-server) release.

That repo has no releases yet, so what's currently published is a committed **placeholder**: a
minimal OpenAPI document and a minimal payload schema. They exist so the site builds and the
URLs stay stable. The fetch is best-effort by design — if the release or the asset is missing,
the build keeps the placeholder and succeeds rather than failing. Once `meshbee-server` cuts its
first release, the real artifacts replace these automatically.
